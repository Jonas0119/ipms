/**
 * +----------------------------------------------------------------------
 * | 开源物业管理系统，敬请使用
 * +----------------------------------------------------------------------
 */

export function hide(phone: string): string {
    return phone ? `${phone.substring(0, 3)}****${phone.substring(7, 11)}` : null;
}
