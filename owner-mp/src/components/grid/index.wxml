<!--
 +----------------------------------------------------------------------
 | 开源物业管理系统，敬请使用
 +----------------------------------------------------------------------
-->
<wxs src="./index.wxs" module="computed" />

<view
    class="cw-grid custom-class {{ border && !gutter ? 'cw-hairline--top' : '' }}"
    style="{{ computed.rootStyle({ gutter }) }}"
>
    <slot />
</view>
