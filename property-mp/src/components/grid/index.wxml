<wxs src="./index.wxs" module="computed" />

<view
    class="ipms-grid custom-class {{ border && !gutter ? 'ipms-hairline--top' : '' }}"
    style="{{ computed.rootStyle({ gutter }) }}"
>
    <slot />
</view>
