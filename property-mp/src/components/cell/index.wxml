<wxs src="../wxs/utils.wxs" module="utils" />
<wxs src="./index.wxs" module="computed" />

<view
    class="custom-class {{ utils.bem('cell', [size, { center, required, borderless: !border, clickable: isLink || clickable, detail: isDetail }]) }}"
    hover-class="ipms-cell--hover hover-class"
    hover-stay-time="70"
    style="{{ customStyle }}"
    bind:tap="onClick"
>
    <ipms-icon wx:if="{{ icon }}" name="{{ icon }}" class="ipms-cell__left-icon-wrap" custom-class="ipms-cell__left-icon" />
    <slot wx:else name="icon" />

    <view style="{{ computed.titleStyle({ titleWidth, titleStyle }) }}" class="ipms-cell__title title-class">
        <block wx:if="{{ title }}">{{ title }}</block>
        <slot wx:else name="title" />

        <view wx:if="{{ label || useLabelSlot }}" class="ipms-cell__label label-class">
            <slot wx:if="{{ useLabelSlot }}" name="label" />
            <block wx:elif="{{ label }}">{{ label }}</block>
        </view>
    </view>

    <view class="ipms-cell__value value-class {{title ? '' : 'only'}}">
        <block wx:if="{{ value || value === 0 }}">{{ value }}</block>
        <slot wx:else />
    </view>

    <ipms-icon
        wx:if="{{ isLink }}"
        name="{{ arrowDirection ? 'arrow' + '-' + arrowDirection : 'arrow' }}"
        class="ipms-cell__right-icon-wrap right-icon-class"
        custom-class="ipms-cell__right-icon"
    />
    <slot wx:else name="right-icon" />

    <slot name="extra" />
</view>
