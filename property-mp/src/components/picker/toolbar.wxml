<view wx:if="{{ showToolbar }}" class="ipms-picker__toolbar toolbar-class">
    <view
        class="ipms-picker__cancel"
        hover-class="ipms-picker__cancel--hover"
        hover-stay-time="70"
        data-type="cancel"
        bindtap="emit"
    >
        {{ cancelButtonText }}
    </view>
    <view wx:if="{{ title }}" class="ipms-picker__title ipms-ellipsis">{{ title }}</view>
    <view
        class="ipms-picker__confirm"
        hover-class="ipms-picker__confirm--hover"
        hover-stay-time="70"
        data-type="confirm"
        bindtap="emit"
    >
        {{ confirmButtonText }}
    </view>
</view>
